# 2026-03-23

## URL 清洗工具改造与增强
- 为 `/Users/miawu/Downloads/url-cleaner (1).html` 添加了"在新标签页打开"功能
- 在三列输出（规则库过滤、清除 Fragment、清除全部参数）的头部都添加了"打开"按钮
- 修复了点击事件问题：将 `onclick="openLink(url)"` 改为直接内联 `onclick="window.open(url, '_blank')"` 以确保在 Chrome 中正常工作

## URL 智能归一化工具（全新增强版）
创建了 `/Users/miawu/Downloads/url-cleaner-enhanced.html` - 全新的智能归一化工具
- **参数重要性自动检测**：基于域名白名单和参数语义分析，自动评分参数重要性（🟢核心/🟡可选/🔴追踪）
- **4种预设归一化方案**：
  - 方案1: 仅保留核心参数（最激进）
  - 方案2: 保留核心+可选参数（推荐）
  - 方案3: 仅删除追踪参数（最保守）
  - 方案4: 删除所有参数
- **自定义方案**：支持手动编辑 URL 并添加到预览
- **一键多页面预览**：使用 iframe 网格同时预览原始 URL + 所有归一化方案
- **快速检查清单**：自动检测 ✓ URL是否可访问 ✓ 页面标题是否一致（受跨域限制）
- **一键刷新**：可同时刷新所有预览页面进行重新验证
- 域名白名单覆盖 YouTube、Amazon、GitHub、Twitter、Bilibili、淘宝等常见网站

### 最新优化（21:03）
- 改进 X-Frame-Options 阻止的处理：5秒超时检测 + 友好提示
- 新增"🗂️ 在新标签页中打开所有方案"功能：一键打开原始URL + 所有方案到浏览器标签页
- 延迟 300ms 逐个打开，避免浏览器弹窗阻止
- 适用于拒绝 iframe 嵌入的网站（如 eltbooks.com）

### 布局重构 v3（21:36）
创建了 `/Users/miawu/Downloads/url-cleaner-v3.html` - 全新的集成布局版本
- **移除标题对比功能**：删除了无法工作的跨域标题检测
- **方案与预览集成**：每个方案卡片下方直接内嵌预览窗口
- **双重复制按钮**：方案头部和预览窗口头部都有复制按钮
- **自动预览**：智能分析后自动显示所有预览，无需额外点击
- **精简控制**：顶部只保留"批量打开"和"刷新预览"两个按钮
- **简化状态**：只显示"加载成功"/"网站拒绝iframe"/"跨域限制"/"加载失败"
- **自定义方案**：添加后自动创建卡片和预览窗口

### 横向瀑布流布局（次日10:39）
- **Grid 网格布局**：使用 CSS Grid 将方案卡片横向排列
- **自适应列数**：根据屏幕宽度自动调整列数（最小 420px/列）
- **一屏对比**：所有预览窗口在一屏内并排显示，方便快速对比
- **响应式优化**：中等屏幕 2-3 列，小屏幕自动切换为单列
- **预览高度统一**：iframe 固定 420px 高度，保持视觉一致性

### 状态提示优化（10:53）
- **简化加载状态**：只要 iframe 触发 onload 就显示"✓ 预览加载成功"
- **移除误导性警告**：不再显示"无法访问内容（跨域限制）"
- **减少留白**：压缩卡片内边距（18px→16px）、URL 框高度（70px→60px）
- **扩大预览区域**：iframe 高度提升到 420px，充分利用空间

### 智能规则学习系统（次日11:02）
创建了完整的规则学习与定制规则库系统：

**核心功能：**
1. **批量上传 URL 对**：支持「原始 URL → 清洗后 URL」格式，自动解析差异
2. **智能规则提取**：
   - 自动分析保留/删除的参数
   - 支持路径级和全局级规则
   - 统计样本数量和更新时间
3. **定制规则存储**：持久化到 localStorage，优先级高于通用规则
4. **规则管理面板**：查看、删除、导出/导入规则库

**技术实现：**
- `customRules` 对象结构：`{ "domain.com": { paths: {}, globalKeep: [], globalRemove: [], samples: 0 } }`
- `applyCustomRules()` 函数：在参数评分前优先检查定制规则
- `learnFromPairs()` 函数：解析 URL 对，提取参数差异，合并规则
- `scoreParamImportance()` 更新：新增 `pathname` 参数，优先使用定制规则

**UI 更新：**
- 新增「🧠 智能规则学习」区域
- 批量输入框支持箭头 → 或 Tab 分隔
- 学习结果实时显示：样本数、规则预览
- 规则管理面板：域名列表、操作按钮
- 版本号更新：v3.0 → v3.1

**备注：**
- 方案3标签增加「参考 ClearURLs 规则」说明

### 双列输入验证优化（次日11:12）
将单一输入框改为双列布局，提升易用性和数据准确性：

**UI 改进：**
1. **双列布局**：
   - 左列：📥 原始 URL（每行一个）
   - 右列：✨ 归一化后 URL（每行一个）
   - 实时显示行数统计（行数匹配时变绿）
2. **增强的验证错误提示**：
   - 行数不匹配：明确显示左右两列的行数差异
   - 域名不匹配：高亮显示可能的串行问题
   - 路径不一致：精确定位问题行号

**验证逻辑：**
- `showError()` 函数：错误信息以红色面板显示，支持多行格式
- `updateLineCounts()` 函数：实时统计并高亮显示行数是否匹配
- `learnFromPairs()` 更新：
  1. 先验证行数是否相等（不等则阻止提交）
  2. 逐行验证域名一致性（不一致则提示串行）
  3. 验证路径一致性
  4. 全部通过后才允许保存规则

**响应式设计：**
- 桌面端：双列并排显示
- 移动端（<1000px）：自动切换为上下堆叠

**技术细节：**
- CSS Grid 布局：`grid-template-columns: 1fr 1fr`
- 输入监听器：`input` 事件实时更新行数
- 错误提示样式：红色边框 + 浅红背景 + 圆角卡片

### 自定义方案可编辑功能（次日13:33）
提升自定义方案的灵活性和易用性：

**核心改进：**
1. **URL 可编辑**：
   - 所有方案卡片的 URL 改为输入框（原始 URL 只读）
   - 自定义方案可直接在卡片中修改 URL
   - 支持实时编辑，无需删除重新添加
   
2. **刷新预览按钮**：
   - 每个可编辑卡片增加「🔄 刷新预览」按钮
   - 修改 URL 后点击刷新，立即加载新页面
   - 支持快速迭代测试不同参数组合

3. **保留原始 URL**：
   - 添加自定义方案后，输入框自动恢复为原始 URL
   - 方便快速创建多个变体方案
   - 如果没有原始 URL，保持当前值不变

**技术实现：**
- `createVariantCard()` 更新：
  - 生成唯一 cardId 标识每个卡片
  - URL 显示改为 `<input>` 元素
  - 原始 URL 卡片添加 `readonly` 属性
  - 动态生成刷新按钮（原始 URL 不显示）
- `refreshSinglePreview(cardId)` 函数：
  - 读取输入框的新 URL
  - 验证 URL 格式
  - 更新 iframe.src 并重新监听加载状态
- `addCustomVariant()` 更新：
  - 添加后恢复为 `currentOriginalUrl`（如果存在）
  - 否则保持当前值，方便继续修改

**UI 样式：**
- `.variant-url-input`：可编辑输入框样式
  - Focus 时蓝色边框高亮
  - readonly 状态灰色背景 + 禁用光标
- `.variant-url-editable`：Flex 布局，输入框 + 刷新按钮并排
